"""NIYAMR AI agent package (copied into with_api for isolation).

This mirrors the top-level `agent` package so `with_api` is self-contained.
"""

__all__ = ["extractor", "summarizer", "sections", "rule_checks", "api_client", "utils", "summarizer_hybrid", "gemini_client"]
